@layout('main')

@section('content')
    <a href="/posts">Go back</a>
    <hr>
    <h1>{{post.title}}</h1>
    <p>{{post.body}}</p>

    <hr>

    <a href="/posts/edit/{{post.id}}" class="btn btn-dark float-left d-block">Edit</a>

    <form action="{{'/posts/' + post.id + '?_method=DELETE'}}" method="POST">
         {{csrfField()}}
        <button class="btn btn-danger d-block float-right">Delete</button>
    </form>
@endsection